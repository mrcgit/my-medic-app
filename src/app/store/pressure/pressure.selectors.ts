import { AppState } from "../store";


export const selectPressureParams = (state: AppState) => state.slice2.items

export const selectPresssureGraphModel = (state: AppState) => state.slice2.model







